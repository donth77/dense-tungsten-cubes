# Tooling scratch
.playwright-mcp/
.DS_Store

# Raw asset staging: multi-MB marketplace downloads, zips and stalled .crdownload files.
# Prepped, scaled, budget-checked outputs are committed under public/models/ instead (09 §7).
assets-lib/

# Build output
node_modules/
dist/

# Internal design docs — local only, not part of the public repo
docs/
.claude/

# Playwright
test-results/
playwright-report/
blob-report/
.playwright-mcp/

test-screenshots
*.png

# ...except the PNGs in public/, which are site assets rather than test output:
# the OG card the deployed site serves, and the README screenshot.
!public/*.png
